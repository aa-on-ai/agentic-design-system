# Museum loan registrar foundation v2 builder report

- Run ID `2026-08-18-foundation-v2`
- Behavior commit `61795131048157fce7696b35d1b202c51ae12256`
- Artifact SHA-256 `2fc99ff5509693f85f332117d247eda43dfa62aa427d9da70dc48f59987cdc5e`
- Evidence SHA-256 `1d33da65ffa58afead24ff8cd92a42de616977c093e70b47ac5b031a30cb55ad`

## Build result

The registrar surface was built fresh from the frozen outcome. Object identity, condition focus, environmental constraints, and custody milestones form one careful record. Four hash states render distinct content at both frozen breakpoints.

The error state natively disables `Approve transfer` while preserving enabled `Download condition report`. Both actions remain enabled in default. The action contract passed in Chromium at both breakpoints.

The independent first-pass review found that empty state still described the active cedar-mask transfer. The repair replaces the shared title and summary with truthful state-specific copy and disables approval outside default. The regrade found the record-specific download still active in empty state, so the same finding was completed by disabling that action when no report exists while preserving it in default and error. The museum matrix was recaptured after each correction.

## Evidence

- Eight screenshots captured at 390x844 and 1280x800
- Eight distinct render signatures
- Zero serious or critical axe violations
- No horizontal overflow, missing main landmark, live-region failure, undersized touch target, or cumulative layout shift failure
- No rounded single-edge border, one-edge shadow, forced uppercase, unusual typography, symbol-only control, status dot, colon-copy, or em-dash candidate
- Ten contextual divider observations across the full matrix, confined to flat fact rows and the custody section

The source heuristics found all four named states but emitted known HTML false positives for responsive detection and icon-led labeled buttons. The conventional fallback font note is informational. Rendered evidence verifies accessibility and responsive behavior directly.

## Builder stop

The frozen outcome and rendered gates are complete. The final closure review verified the prior independent finding against the repaired DOM and regenerated screenshot matrix.
