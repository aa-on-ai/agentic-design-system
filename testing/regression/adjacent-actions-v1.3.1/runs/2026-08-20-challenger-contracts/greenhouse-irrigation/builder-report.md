# Glasshouse irrigation challenger-contract rerun

The frozen artifact was freshly captured with behavior digest `c0a58b0d`. All eight rendered gates passed, the report-only modal and hover diagnostics were clear, and all eight strict pixel comparisons matched the prior passing packet. The Foundation v2 builder report below remains the artifact's source receipt.

# Glasshouse irrigation foundation v2 builder report

- Run ID `2026-08-18-foundation-v2`
- Behavior commit `61795131048157fce7696b35d1b202c51ae12256`
- Artifact SHA-256 `d8d5478df26e3b5ba9a35932b6212e733af8ba034a7244881a344d501a8eb1b3`
- Evidence SHA-256 `962c1b188355a8a5b0c959c5aed5128d0d7ea6bdd3ede111eb2ee4538eed1235`

## Build result

The irrigation surface was built fresh from the frozen outcome. It connects the next watering run, crop moisture, an active probe exception, and human escalation without a metric-card wall. Four hash states render distinct content at both frozen breakpoints.

The error state natively disables `Adjust schedule` while preserving enabled `Call grower`. Both actions remain enabled in default. The action contract passed in Chromium at both breakpoints.

The independent first-pass review found that empty state still announced an upcoming run. The repair gives each state truthful summary copy, then recaptures the complete matrix.

## Evidence

- Eight screenshots captured at 390x844 and 1280x800
- Eight distinct render signatures
- Zero serious or critical axe violations after correcting progressbar semantics and muted-text contrast
- No horizontal overflow, missing main landmark, live-region failure, undersized touch target, or cumulative layout shift failure
- No rounded single-edge border, one-edge shadow, forced uppercase, unusual typography, symbol-only control, status dot, colon-copy, or em-dash candidate
- Eight contextual divider observations across the full matrix, confined to flat fact and mobile section boundaries

The source heuristics found all four named states but emitted known HTML false positives for responsive detection and icon-led labeled buttons. Rendered evidence verifies those behaviors directly.

## Builder stop

The frozen outcome and rendered gates are complete. Final quality judgment is reserved for a clean evidence-only grader context.
