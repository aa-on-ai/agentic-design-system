# ADS MCP 2026-07-28 migration report

Date: 2026-07-29  
Branch: `agent/ads-mcp-2026-07-28`  
Baseline: `7de1fd148bfb1035d68c1fa5ddb14e9dc3911e51`

## Result

The local release candidate now uses the MCP TypeScript SDK v2 split packages and one
`serveStdio` binary for both protocol eras. The public tool contract remains exactly
`ads_render`, `ads_evaluate`, and `ads_trace`.

The package also advertises the stable MCP Apps extension and serves a self-contained inline
evidence review at `ui://ads/review`. Run handles remain filesystem-backed application identifiers
and recover across fresh MCP client, server, and service instances.

No npm publication, Registry write, Git push, public-site change, or deployment occurred.

## Architecture decisions

- Protocol: support legacy `initialize` and modern `server/discover` with MCP `2026-07-28`.
- SDK: replace `@modelcontextprotocol/sdk` v1 with `@modelcontextprotocol/client` and
  `@modelcontextprotocol/server` v2.
- State: preserve `<root>/.ads/runs/<runId>` as the durable run spine; never depend on an MCP
  session identifier.
- Apps: advertise `io.modelcontextprotocol/ui` with the required MCP App MIME type, attach
  `_meta.ui.resourceUri` to the three tools, and keep meaningful text/structured fallbacks.
- App lifecycle: implement `ui/initialize`, `ui/notifications/initialized`, tool result and
  cancellation notifications, host context changes, server tool/resource proxy calls,
  size changes, model-context updates, ping, and teardown over JSON-RPC `postMessage`.
- Tasks: do not advertise `io.modelcontextprotocol/tasks`. Current calls are bounded and
  synchronous; the deprecated core Tasks API is not reused.
- Auth: keep local stdio unauthenticated. A remote transport remains blocked on current
  OAuth/OIDC implementation.
- Persistence: local recovery is verified. Hosted or replicated operation remains blocked on a
  shared durable store and concurrency controls.

## Verification

- `npm run typecheck`: passed.
- `npm test`: 28 tests passed, 0 failed.
- Compiled stdio legacy sequence: render, evaluate, trace, and resources passed through
  `initialize`.
- Compiled stdio modern sequence: the same sequence passed through `server/discover` pinned to
  `2026-07-28`.
- Cross-server recovery: a fresh client/server/service pair evaluated and traced a prior run.
- MCP App browser acceptance: handshake, tool result, resource read, evaluation action, retained
  evidence, size notification, and serious/critical axe scan passed in Chromium.
- Packed consumer acceptance: a clean temporary install of the generated tarball completed the
  modern three-tool sequence and read `text/html;profile=mcp-app`.
- Package contents: `npm pack --dry-run` included `dist/app/review.html`.
- Dependency check: runtime resolves `@modelcontextprotocol/client@2.0.0` and
  `@modelcontextprotocol/server@2.0.0`; the v1 monolithic SDK is absent.
- `npm audit --omit=dev`: 0 known vulnerabilities.
- Visual acceptance: the current compiled app rendered successfully in Chromium desktop and
  WebKit mobile.
- Static design checks: state and accessibility checks passed. The generic anti-pattern script
  emitted one "No responsive breakpoints" warning because it only recognizes Tailwind breakpoint
  prefixes; the app uses a native `@media (max-width: 720px)` rule and passed the WebKit mobile
  rendering check.
- `git diff --check`: passed.

Visual receipts:

- `/Users/moltbot/clawd/memory/artifacts/ads-mcp-2026-07-28/mcp-app-chromium-desktop.png`
- `/Users/moltbot/clawd/memory/artifacts/ads-mcp-2026-07-28/mcp-app-webkit-mobile.png`

## Remaining release gates

- Run the real host matrix against Apps-capable production hosts. Local browser harnesses do not
  prove each host's sandbox proxy, consent UI, or tool/resource proxy behavior.
- Run MCP Inspector against the packed package.
- Choose a release version, then update public install strings and release notes.
- Publish to npm, update the MCP Registry, push the branch, and deploy public-site claims only
  after explicit approval.

## Sources

- MCP `2026-07-28` release candidate:
  <https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/>
- TypeScript SDK v2 migration guidance:
  <https://ts.sdk.modelcontextprotocol.io/v2/migration/support-2026-07-28>
- Stable MCP Apps specification:
  <https://github.com/modelcontextprotocol/ext-apps/blob/main/specification/2026-01-26/apps.mdx>
- MCP Tasks extension overview:
  <https://modelcontextprotocol.io/extensions/tasks/overview>

## Failed checks

None. One static responsive warning was verified as a checker limitation, not a product failure.
