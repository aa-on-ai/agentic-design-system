# ADS MCP 0.3.0 release-candidate report

Date: 2026-07-29
Branch: `agent/ads-mcp-2026-07-28`
Package: `ads-mcp@0.3.0`

## Result

The local `0.3.0` release candidate is packaged and passes the complete MCP Inspector CLI contract.
An authenticated Claude Desktop acceptance run on 2026-08-03 also rendered the inline MCP App and
completed render, evaluation, and provenance-backed trace validation. No Git push, npm publication,
MCP Registry write, public-site deployment, or release announcement occurred.

`0.3.0` is a minor release because it adds SDK v2 dual-era protocol support, session-independent
run recovery, and the stable MCP Apps review surface while preserving the three-tool contract.

## Exact package

- Archive: `ads-mcp-0.3.0.tgz`
- Size: 333,454 bytes
- SHA-1: `2deb7cd0cdd58f349f7ad4879a2c984b26962222`
- SHA-256: `f9016447b228c79085312d3150bebbdaf613013fac2bf66a51ecd82f54f36728`
- Integrity:
  `sha512-aLFkrTjGozXTszX9+jZrn5w/QIgQd+AaMmcSLhv95htJxDvAeuTt+AfbdUCnjLV7jSD804/Zw94LB3AaUDgcbA==`

## Verification

- `npm run typecheck`: passed.
- `npm test`: 28 passed, 0 failed.
- `npm run mcp:package-smoke`: passed from a clean packed-package consumer.
- `npm run mcp:cold-start`: passed in 1,989 ms against a 30,000 ms budget.
- Clean consumer install: 0 known vulnerabilities.
- `mcp-publisher validate server.json`: passed against the live MCP Registry validator.
- Targeted lint passed for the three public-site files whose release strings changed.
- MCP Inspector 2.0.0 discovered exactly `ads_render`, `ads_evaluate`, and `ads_trace`.
- All three tools advertised `ui://ads/review` with
  `text/html;profile=mcp-app`, model/app visibility, an empty-domain CSP, and a bordered host
  preference.
- Final Inspector run: `run_ms67dpix_61e54ca2a7a2`.
- `ads_render`: complete at `390x844` and `1280x800`.
- Deterministic gates: 0 serious axe violations, no overflow, landmark, live-region, touch-target,
  or CLS failures.
- `ads_evaluate`: `needs_human`, as expected without a configured judge.
- `ads_trace`: valid with no errors.
- Inspector read the inline app, manifest, evidence, two PNGs, receipt, report, trace, and trace
  validation from the packed package.

## Real-host gate

Authenticated Claude Desktop acceptance passed on 2026-08-03 against the exact packed release
candidate. Run `run_msdok39b_75e7491d4ced` completed at `390x844` and `1280x800`, returned
`needs_human` with judge mode `none`, rendered the inline review UI, and validated a
provenance-backed trace with zero errors. The manifest SHA-256 was
`68972830f9d4a367060e6d2d76d107a6f2302762c4e00df58274a7f1c723d628`.

The first trace dispatch stalled inside Claude Desktop before reaching the MCP server. After a
full Desktop restart, a trace-only recovery call against the durable run handle returned
`valid: true`. The server logs showed normal replies for every request it received.

Inspector 2.0.0's web UI still cannot preview the app because its published npm package omits
`clients/web/static/sandbox_proxy.html`. This is an Inspector packaging blocker, not an ADS server
failure; Inspector CLI and Claude Desktop host acceptance passed independently.

## Failed checks

- The first package smoke run exposed stale v1 SDK imports in three smoke helpers. They were
  migrated to `@modelcontextprotocol/client` and `@modelcontextprotocol/client/stdio`; package and
  cold-start smoke then passed.
- The first targeted demo lint invocation found that the worktree's demo dependencies were not
  installed. A clean `npm ci` restored the declared environment and the same lint check passed.
- Two preliminary Inspector trace calls were rejected because their claimed excerpts were not
  verbatim source text. The lane was reset and the final run passed with verified excerpts.
- Inspector web app preview is blocked by its missing packaged sandbox proxy.
- The first Claude Desktop trace dispatch stalled before reaching the server. A full Desktop
  restart recovered the trace-only call without repeating render or evaluation.
