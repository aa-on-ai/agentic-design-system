name: Release gate

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*"

jobs:
  release-gate:
    runs-on: ubuntu-22.04
    container:
      image: mcr.microsoft.com/playwright:v1.60.0-jammy
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm ci --prefix demos
      - run: npm run release:check
      - name: Require a frozen-suite packet for ADS behavior changes
        env:
          REGRESSION_BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: npm run regression:gate -- --base "$REGRESSION_BASE_SHA"
      - run: npm run build --prefix demos
      - name: Verify public demos in Chromium and WebKit
        run: |
          npm --prefix demos run start -- --hostname 127.0.0.1 --port 3000 > "$RUNNER_TEMP/ads-demo.log" 2>&1 &
          demo_pid=$!
          trap 'kill "$demo_pid"' EXIT

          ready=0
          for attempt in {1..30}; do
            if curl --fail --silent http://127.0.0.1:3000 > /dev/null; then
              ready=1
              break
            fi
            sleep 1
          done

          if [[ "$ready" -ne 1 ]]; then
            cat "$RUNNER_TEMP/ads-demo.log"
            exit 1
          fi

          npm run pawprint:states -- http://127.0.0.1:3000
          ADS_BROWSER=webkit npm run pawprint:states -- http://127.0.0.1:3000
          npm run homepage:hardening -- http://127.0.0.1:3000
          npm run homepage:mobile-nav -- http://127.0.0.1:3000
          npm run homepage:blend -- http://127.0.0.1:3000
          ADS_SECTION_BLEND_BROWSER=webkit npm run homepage:blend -- http://127.0.0.1:3000
      - run: npm ci --prefix packages/ads-mcp
      - run: npm run mcp:cold-start
      - run: npm run mcp:test
      - run: npm run mcp:package-smoke
